const inquirer = require('inquirer');
const Employee = require('../lib/Employee');

class Generator {
    constructor() {

    }
    // to start generator
    initializeGenerator() {
        inquirer
        .prompt({
            // to get employee info
        })
    }
}